import styled from 'styled-components';

const Todo = () => {
  return <StyledTodo>sasdsad</StyledTodo>;
};

const StyledTodo = styled.div`
  height: calc(100vh - 80px);
  padding: 35px;
`;

export default Todo;
