import styled from 'styled-components';

const SignIn = () => {
  return <StyledSignIn className="flex-center"></StyledSignIn>;
};

const StyledSignIn = styled.div`
  height: calc(100vh - 80px);
`;

export default SignIn;
